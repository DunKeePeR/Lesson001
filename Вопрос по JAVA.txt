На странице:
https://math.sgu.ru/sites/chairs/prinf/materials/java/lesson3.htm

имеется раздел "Модификаторы видимости". В конце данного раздела можно нажать "Узнать больше об инкапсуляции".
Там будет сказано о возможности переписать конструктор таким образом, чтобы он осуществлял проверку:

"Заметим напоследок, что в нашем примере неправильный возраст может «прорваться» через конструктор при создании нового объекта. Никто ведь не мешает написать:

Dog dog1 = new Dog("Тузик", 2000);
Для того, чтобы этого не случилось, необходимо переписать конструктор:

public Dog(String n, int a) {
name = n;
age = setAge(a);
}
Теперь проверка осуществляется в конструкторе. Попытка завести в программе собаку с явно некорректными данными не увенчается успехом. Если возраст будет меньше 0 или больше 30, присваивание не выполнится и атрибут age будет иметь значение по умолчанию (для типа int это 0). Такая вот маленькая собачка..."


Вопрос у меня такой:
Речь о файле OOP_001.java (файл не принимается компилятором, с ошибкой. Для отработки необходимо изменить "age = setAge(a);" на "age = a;"
Когда переписываю строчку в конструкторе "на age = setAge(a);", то компилятор ругается: "Error:(17, 21) java: incompatible types: void cannot be converted to int".
Что делаю не так? Как это можно поправить?
При этом вызов метода "dog1.setAge(20);" работает корректно.